spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dormitory_management?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update  # ho?c 'validate', 'create', 'create-drop' t�y v�o m?c ?�ch s? d?ng
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect  # ho?c MySQL5Dialect n?u b?n d�ng MySQL 5

server:
  port: 8080  # C?ng m� ?ng d?ng Spring Boot s? ch?y, c� th? thay ??i n?u c?n
